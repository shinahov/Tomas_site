const loader=document.querySelector('.loader');addEventListener('load',()=>setTimeout(()=>loader.classList.add('done'),700));
const cursor=document.querySelector('.cursor');addEventListener('pointermove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'});document.querySelectorAll('a,button,figure').forEach(x=>{x.onmouseenter=()=>cursor.classList.add('hot');x.onmouseleave=()=>cursor.classList.remove('hot')});
const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.15});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));
const paras=[...document.querySelectorAll('.parallax')];addEventListener('scroll',()=>{document.querySelector('.progress').style.width=scrollY/(document.body.scrollHeight-innerHeight)*100+'%';paras.forEach(x=>{const r=x.parentElement.getBoundingClientRect();x.style.transform=`translateY(${(r.top+r.height/2-innerHeight/2)*-.05}px) scale(1.02)`})},{passive:true});
document.querySelectorAll('.magnetic').forEach(b=>{b.onpointermove=e=>{const r=b.getBoundingClientRect();b.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.18}px,${(e.clientY-r.top-r.height/2)*.18}px)`};b.onpointerleave=()=>b.style.transform=''})
