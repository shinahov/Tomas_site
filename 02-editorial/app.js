document.querySelector('#theme').onclick=()=>document.body.classList.toggle('invert');
const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.18});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));
const tilt=document.querySelector('.tilt');tilt.onpointermove=e=>{const r=tilt.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;tilt.style.transform=`perspective(900px) rotateY(${x*8}deg) rotateX(${-y*8}deg)`};tilt.onpointerleave=()=>tilt.style.transform='';
const floating=document.querySelector('.floating'),floatImg=floating.querySelector('img');document.querySelectorAll('.catalog-list article').forEach(a=>{a.onmouseenter=()=>{floatImg.src=a.dataset.image;floating.classList.add('show')};a.onmouseleave=()=>floating.classList.remove('show')});addEventListener('pointermove',e=>{floating.style.left=e.clientX+25+'px';floating.style.top=e.clientY-170+'px'});
const horizontal=document.querySelector('.horizontal'),pin=document.querySelector('.pin');addEventListener('scroll',()=>{const r=horizontal.getBoundingClientRect(),range=horizontal.offsetHeight-innerHeight,p=Math.max(0,Math.min(1,-r.top/range));pin.style.transform=`translateX(${-p*300}vw)`},{passive:true});
