const canvas=document.querySelector('#field'),ctx=canvas.getContext('2d');let pts=[];function size(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:80},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);ctx.strokeStyle='#d7ff3f18';pts.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;pts.slice(i+1).forEach(q=>{const d=Math.hypot(p.x-q.x,p.y-q.y);if(d<130){ctx.globalAlpha=1-d/130;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}})});ctx.globalAlpha=1;requestAnimationFrame(draw)}size();draw();addEventListener('resize',size);
const screens=[...document.querySelectorAll('.screen')],dots=[...document.querySelectorAll('aside button')];const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){const i=screens.indexOf(e.target);dots.forEach((d,j)=>d.classList.toggle('active',i===j));e.target.querySelectorAll('[data-count]').forEach(runCount);e.target.querySelectorAll('[data-scramble]').forEach(scramble)}}),{threshold:.6});screens.forEach(x=>io.observe(x));
function runCount(el){if(el.dataset.done)return;el.dataset.done=1;const target=+el.dataset.count,start=performance.now();requestAnimationFrame(function f(t){const p=Math.min(1,(t-start)/1200);el.textContent=String(Math.floor(target*p)).padStart(el.textContent.length,'0');if(p<1)requestAnimationFrame(f)})}function scramble(el){if(el.dataset.done)return;el.dataset.done=1;const final=el.innerHTML,chars='ABCDEFGHIJKLMNOPQRSTUVWXYZАБВГДЕЖЗИКЛМНОПРСТУФХЦЧШЩЭЮЯ0123456789';let n=0;const id=setInterval(()=>{el.innerHTML=final.replace(/[A-ZА-ЯЁ]/g,c=>Math.random()<n/14?c:chars[Math.floor(Math.random()*chars.length)]);if(++n>14){el.innerHTML=final;clearInterval(id)}},45)}
document.querySelectorAll('[data-go]').forEach(x=>x.onclick=()=>screens[+x.dataset.go].scrollIntoView({behavior:'smooth'}));const panel=document.querySelector('.index-panel');document.querySelector('#menu').onclick=()=>panel.classList.add('open');document.querySelector('.close').onclick=()=>panel.classList.remove('open');panel.querySelectorAll('[data-go]').forEach(x=>x.addEventListener('click',()=>panel.classList.remove('open')));
document.querySelectorAll('.tilt').forEach(card=>{card.onpointermove=e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(800px) rotateY(${x*12}deg) rotateX(${-y*12}deg) translateZ(12px)`};card.onpointerleave=()=>card.style.transform=''});document.querySelector('.portal').onpointermove=e=>{const x=e.clientX/innerWidth*100,y=e.clientY/innerHeight*100;document.querySelector('.portal-mask').style.background=`radial-gradient(circle at ${x}% ${y}%,transparent 0 24%,#050706 25% 100%)`};
